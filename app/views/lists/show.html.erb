<nav><%= link_to "All Lists", user_path(current_user) %>
<h1><%= @list.name %></h1>

  <ul>
    <%= render partial: "item", collection: @items %>
  </ul>


<%= link_to "Edit List", edit_list_path%><br />
<%= link_to "Add New Item", new_list_item_path(@list), { :class=>"new-item-link" } %>


<center>
  <% if flash[:danger] %>
   <div class="danger"><%= flash[:danger] %></div>
 <% end %>
</center>
<div class="new-item-form">
  <div class="panel panel-default">
    <h1 class="panel-heading">Add Item</h1>
      <%= render partial: "/items/form", locals: { list: @list, item: @item} %>
  </div>
</div>
